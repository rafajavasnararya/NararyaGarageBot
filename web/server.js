import express from "express";import path from "node:path";const app=express();app.use(express.json());app.use(express.static(path.resolve("web/public")));app.get("/api/health",(_,r)=>r.json({ok:true,service:"NARARYA BUSINESS WEB"}));app.get("/api/brands",(_,r)=>r.json([{id:"studio",name:"NARARYA STUDIO"},{id:"garage",name:"NARARYA GARAGE"},{id:"hilekros",name:"HILEKROS PRODUCTS"}]));app.listen(process.env.WEB_PORT||8080,()=>console.log("Dashboard online"));