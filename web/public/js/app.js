async function load(){const [b,h]=await Promise.all([fetch("/api/brands"),fetch("/api/health")]);const brands=await b.json();const health=await h.json();document.querySelector("#brands").innerHTML=brands.map(x=>'<article class="card"><h2>'+x.name+'</h2><p class="brand">Business Unit</p><button onclick="alert(\'Selected: '+x.id+'\')">Open</button></article>').join("");document.querySelector("#health").textContent=JSON.stringify(health,null,2)}load().catch(e=>document.querySelector("#health").textContent=e.message);